#include <stdio.h>

int main() {
    float a = 3.333333;
    // We use (type)var to cast.
    printf("Value before casting: %f\n", a);
    printf("Value after casting: %d\n", (int)a);


    printf("%f", 10/3);
}